install prom + grafana 
	=> container metric can be display ✅

install tempo
	=> grafana can connect to tempo ✅

install loki
	=> grafana can connect to loki ✅

install alloy
	=> make a simple webserver, use k6 to simulate span metric (with random error)
	=> use the webserver to send logs to alloy
	=> grafana can see traces, logs, and span-metric
