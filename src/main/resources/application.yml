server:
  port: 8080

spring:
  application:
    name: demo-app

management:
  tracing:
    enabled: true
    sampling:
      probability: 1.0
  otlp:
    tracing:
      endpoint: ${TEMPO_PUSH_API_ENDPOINT}
    metrics:
      export:
        enabled: false